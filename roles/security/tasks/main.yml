- name: Enable UFW
  ufw:
    state: enabled
    policy: deny

- name: Allow SSH/HTTP/HTTPS
  ufw:
    rule: allow
    port: "{{ item }}"
  loop: [22, 80, 443]

- name: Enable fail2ban
  systemd:
    name: fail2ban
    enabled: yes
    state: started
