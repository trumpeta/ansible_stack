- name: Install MariaDB server packages
  ansible.builtin.apt:
    name: "{{ mariadb_packages }}"
    state: present

- name: Ensure MariaDB is running
  ansible.builtin.systemd:
    name: "{{ mariadb_service_name }}"
    state: started
    enabled: yes

- name: Create SQL bootstrap script for application database
  ansible.builtin.copy:
    dest: /root/ansible_db_setup.sql
    mode: "0600"
    content: |
      CREATE DATABASE IF NOT EXISTS `{{ db_name }}` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
      CREATE USER IF NOT EXISTS '{{ db_user }}'@'localhost' IDENTIFIED BY '{{ db_pass_sql_escaped }}';
      ALTER USER '{{ db_user }}'@'localhost' IDENTIFIED BY '{{ db_pass_sql_escaped }}';
      GRANT ALL PRIVILEGES ON `{{ db_name }}`.* TO '{{ db_user }}'@'localhost';
      FLUSH PRIVILEGES;

- name: Check if database already exists
  ansible.builtin.shell: >
    mysql --protocol=socket --socket={{ mariadb_socket }}
    -e "SELECT SCHEMA_NAME FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME='{{ db_name }}';"
  register: db_check
  changed_when: false
  failed_when: false

- name: Initialize application database and user (skip if exists)
  ansible.builtin.shell: mysql --protocol=socket --socket={{ mariadb_socket }} < /root/ansible_db_setup.sql
  when: db_check.stdout == ""
  changed_when: true
